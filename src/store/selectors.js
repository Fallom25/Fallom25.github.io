const getGeolocation = (state) => state.geolocation;
const getWeatherInfo = (state) => state.weatherInfo;
const getFormValues = (state) => state.formValues;

export { getGeolocation, getWeatherInfo, getFormValues };
