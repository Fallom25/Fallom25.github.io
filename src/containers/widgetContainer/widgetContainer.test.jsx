import React from 'react';
import { WidgetContainer } from './WidgetContainer';
import { render, cleanup } from '../../test-utils';


describe('Testing the WidgetContainer', () => {
  afterEach(cleanup);

  it('renders WidgetContainer', () => {
    //snapshot
  });
});